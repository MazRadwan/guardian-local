# Epic 19.5 Review Log

## Round 1 - Initial Plan Review

**Date:** 2026-01-14
**Reviewer:** GPT-5.2 via codex MCP
**Status:** Approved with conditions

### GPT Feedback Summary

**Approval:** Yes, with conditions

**Key Recommendations:**

1. **Single input path**: Use dropzone's `getInputProps()` input and have paperclip trigger it - avoid two separate inputs/paths
2. **Dropzone config**: Add `noClick: true` and `noKeyboard: true` to prevent hijacking textarea focus/shortcuts
3. **Skip wrapper hook**: Generic wrapper is unnecessary abstraction over stable library API - use react-dropzone directly in Composer
4. **Accessibility**: Add non-color cues (text), `aria-live` for rejection, focus styles
5. **Test strategy**: Prioritize integration tests on Composer behavior, skip brittle wrapper unit tests
6. **Security**: Ensure constraints mirror server validation, treat filenames as untrusted
7. **Story merge**: Merge 19.5.0.2 and 19.5.0.3 to reduce churn on Composer.tsx

### Action Items

- [x] Remove Story 19.5.0.1 wrapper hook (use react-dropzone directly)
- [x] Merge Story 19.5.0.2 and 19.5.0.3 into single story
- [x] Update technical approach per GPT recommendations
- [x] Add accessibility requirements to acceptance criteria

### Revised Story Structure

| Original | New | Reason |
|----------|-----|--------|
| 19.5.0.1 Install & wrapper | 19.5.1 Install dependency only | Wrapper unnecessary |
| 19.5.0.2 Integrate | 19.5.2 Integrate + visual + tests | Merged per GPT |
| 19.5.0.3 Visual + tests | (merged above) | - |

### Resolution

**Accepted all recommendations.** GPT's feedback reduces complexity and improves maintainability.

---

## Round 2 - Implementation Code Review

**Date:** 2026-01-14
**Reviewer:** GPT-5.2 via codex MCP
**Status:** Approved with minor recommendations (no blockers)

### Stories Reviewed
- 19.5.1: Install react-dropzone dependency
- 19.5.2: Integrate dropzone with Composer (visual + tests)

### GPT Recommendations

1. **LOW: Remove unused imports** - `useCallback` in Composer.tsx if present, `userEvent` in test file
2. **LOW: Explicit hidden class** - Add `className="hidden"` to dropzone input instead of relying on getInputProps()
3. **LOW: Clamp maxFiles defensively** - Use `Math.max(0, 10 - files.length)` to avoid negative values
4. **LOW: Prevent toast spam** - Cap or summarize rejection messages for large invalid drops
5. **LOW: Improve test selectors** - Use `data-testid` instead of brittle `.max-w-3xl > div` selector
6. **LOW: Add textarea focus test** - Ensure dropzone wrapper doesn't interfere with clicking/focusing textarea

### Fixes Applied
- [x] Added `className="hidden"` to dropzone input
- [x] Added `Math.max(0, ...)` clamping to maxFiles
- [ ] Toast spam prevention (deferred - edge case)
- [ ] Test selector improvement (deferred - tests passing)
- [ ] Textarea focus test (deferred - tests passing)

### Resolution

**APPROVED** - All critical items pass. Minor improvements applied, remaining items deferred as non-blocking.

---

## Final Approval

**Epic 19.5 plan approved** after incorporating GPT recommendations.
**Implementation approved** after code review.
