## Story 14.2.1 – Generate Button Styling

Status: Complete

### Goal
Style the generate button to match the Appendix reference (color, padding, hover/focus states) while staying inside the standard chat message layout. Styling reference: `tasks/epic-14/Appendix-example-code.md`.

### Requirements
- Use primary dark button styling per Appendix example (no bespoke card).
- Maintain loading/disabled states (`Generating...` with spinner).
- Keep data-testid `generate-questionnaire-btn`.
- Do not change placement logic; only adjust styling.

### Acceptance Criteria
- Generate button visually matches the reference styles (primary dark, rounded, correct padding).
- Hover/focus/disabled states present and accessible.
- Still rendered within the assistant message content (not a separate card).

### Files & Changes
- `apps/web/src/components/chat/QuestionnairePromptCard.tsx` – adjust button classes/variant; keep logic intact.
- Shared button theme overrides if needed (minimize surface area).

### Testing
- Component snapshot/DOM assertions for class names and disabled/loading state.
