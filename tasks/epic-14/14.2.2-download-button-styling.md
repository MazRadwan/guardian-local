## Story 14.2.2 – Download Button Styling

Status: Complete

### Goal
Style the download buttons to match the Appendix reference: primary first action, secondary text/ghost for others, within the standard chat message layout. Styling reference: `tasks/epic-14/Appendix-example-code.md`.

### Requirements
- First download option rendered as primary (dark) with icon; others as secondary/ghost per reference.
- Respect existing formats array ordering and data-testids (`download-{format}-btn`).
- Maintain accessibility states (focus/hover/disabled).
- Keep layout inline with chat content; no special card container.

### Acceptance Criteria
- Visual match to Appendix for primary + secondary download buttons.
- No regression to spacing/flow inside assistant message.
- Buttons remain functional for all supported formats.

### Files & Changes
- `apps/web/src/components/chat/QuestionnairePromptCard.tsx` – adjust button variants/classes for download buttons; keep logic unchanged.
- Shared button theme overrides if needed.

### Testing
- Component snapshot/DOM: primary vs secondary class differences; hover/focus states.
- Interaction: click handlers still fire with correct format.
