## Story 14.2.3 – Tests for Button Styling

Status: Complete (tests included in 14.2.1 and 14.2.2, updated for 14.1.5 gating)

### Goal
Validate generate and download button styling/states match the Appendix reference and remain functional within the chat message layout.

### Requirements
- Snapshot/DOM checks for class names/variants on generate and download buttons.
- Verify primary vs secondary styling for downloads (icon on primary).
- Hover/focus/disabled/loading states asserted where feasible.
- Ensure data-testids remain intact.

### Acceptance Criteria
- Tests fail with old styling and pass after updates.
- No regression to button functionality or placement inside the assistant message.
- Adjusted for 14.1.5: download buttons are asserted only after the “stream complete”/visibility gate that places the download bubble below the rendered questionnaire.

### Files & Changes (expected)
- `apps/web/src/components/chat/__tests__/QuestionnairePromptCard.test.tsx`
- Additional targeted tests if needed for styling assertions.

### Testing
- Run relevant web component tests after styling changes.
- Update tests to simulate “questionnaire stream complete” (or visibility flag) before expecting download buttons, per Story 14.1.5.
