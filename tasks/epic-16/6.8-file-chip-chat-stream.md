# Epic 16.6.8: File Chip Chat Stream Integration

## Overview

After Sprint 16.6.1, the FileChip displays inside the composer during upload/processing. However, once the user sends a message, the chip **remains stuck in the composer** instead of moving to the chat stream as part of the user's message.

This story aligns Guardian's file upload UX with industry standards (ChatGPT, Claude.ai) where uploaded files appear as message attachments in the conversation history.

---

## Current Behavior (Bug)

**Screenshot Reference:** `/Users/mazradwan/Desktop/Screenshot 2025-12-17 at 10.39.38 AM.png`

```
┌─────────────────────────────────────────────────────────────┐
│  Chat Messages                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ Assistant: Thank you! I can see this is the NLHS...    │ │
│  │ ...                                                    │ │
│  └────────────────────────────────────────────────────────┘ │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐│
│  │ ┌──────────────────────────────────┐                    ││
│  │ │ ✓ NLHS Product Requireme...  ×   │ ← STUCK HERE!     ││
│  │ │   Ready                          │                    ││
│  │ └──────────────────────────────────┘                    ││
│  │                                                         ││
│  │ Type a message...                                       ││
│  │ [Assessment ▾] [📎]                            [Send]   ││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
```

**Problems:**
1. FileChip remains in composer after message is sent
2. User has no visual record of which file they uploaded in the conversation
3. Dark chip theme (bg-gray-800) clashes with light app theme
4. Cannot click chip to download the original document

---

## Target Behavior (GPT Reference)

**Screenshot Reference:** `/Users/mazradwan/Desktop/Screenshot 2025-12-17 at 10.14.42 AM.png`

```
┌─────────────────────────────────────────────────────────────┐
│  Chat Messages                                              │
│                                                             │
│                    ┌───────────────────────────────────────┐│
│                    │ 📄 mermaid-diagram-dec-5.txt         ││ ← File as user message
│                    │    Document                           ││    (right-aligned)
│                    └───────────────────────────────────────┘│
│                                                    thoughts ││
│  ┌────────────────────────────────────────────────────────┐ │
│  │ Thought for 16s                                        │ │
│  │ Framework is active.                                   │ │
│  │ I've got your Mermaid architecture diagram loaded...   │ │ ← Assistant response
│  └────────────────────────────────────────────────────────┘ │   appears BELOW file
│                                                             │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────────────────────────────────────────────────┐│
│  │ Message input (no file chip - it moved to chat)        ││
│  └─────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
```

**Key Patterns:**
1. File appears as **user message bubble** (right-aligned)
2. Light theme (white/gray background with subtle styling)
3. Shows filename + "Document" type label
4. Blue document icon
5. **Clickable** - downloads the file when clicked
6. New messages appear **below** the file (not sticky)

---

## Requirements

### 1. File Chip Moves to Chat Stream on Send

When user clicks Send (with or without text message):
- FileChip state is cleared from composer
- File attachment metadata is included with the user message
- MessageBubble renders the file attachment as part of the user message

### 2. Light Theme for FileChip

Restyle FileChip to match app's light theme:

| Property | Current (Dark) | Target (Light) |
|----------|----------------|----------------|
| Background | `bg-gray-800` | `bg-gray-100` or `bg-white border` |
| Text | `text-white` | `text-gray-900` |
| Icon | White icons | `text-blue-500` or `text-gray-600` |
| X button | `text-gray-400` | `text-gray-400 hover:text-gray-600` |
| Error bg | `bg-red-900` | `bg-red-50 border-red-200` |

### 3. File Chip in Chat Stream (MessageBubble)

Create a chat-stream variant of FileChip for rendering in MessageBubble:
- Displayed inside user message bubble (right-aligned)
- No X button (file is already sent, can't remove)
- No progress bar (already complete)
- **Clickable** - triggers file download

### 4. Download Functionality

When file chip in chat stream is clicked:
- Fetch file from backend (existing `/api/documents/:id/download` or similar)
- Trigger browser download with original filename

### 5. Not Sticky

File attachment message flows with conversation:
- New assistant responses appear below it
- User can scroll up to see previous file attachments
- No special pinning/sticky behavior

---

## Technical Implementation

### Data Flow

```
Current:
  Upload completes → FileChip shows "Ready" in composer → stays forever

Target:
  Upload completes → FileChip shows "Ready" in composer
  User sends message → Message includes { attachments: [{ fileId, filename }] }
                    → FileChip cleared from composer
                    → MessageBubble renders with FileAttachment component
```

### Schema Changes

**Message entity** - add optional attachments field:
```typescript
// packages/backend/src/domain/entities/Message.ts
export interface MessageAttachment {
  fileId: string;
  filename: string;
  mimeType: string;
  size: number;
  storagePath: string;
}

export interface Message {
  // ... existing fields
  attachments?: MessageAttachment[];
}
```

**Database schema** - add attachments column:
```typescript
// packages/backend/src/infrastructure/database/schema/messages.ts
attachments: jsonb('attachments').$type<MessageAttachment[]>(),
```

### Backend Changes

1. **Message creation** - Accept attachments in send message payload
2. **File download endpoint** - Serve stored files (may already exist)

### Frontend Changes

1. **Composer.tsx** - Pass attachment info when sending message, clear chip after send
2. **useFileUpload.ts** - Expose `getAttachment()` method to get file metadata for message
3. **FileChip.tsx** - Restyle to light theme
4. **FileChipInChat.tsx** - New component for chat stream (no X, clickable)
5. **MessageBubble.tsx** - Render file attachments from message data

---

## Story Breakdown

### Story 6.8.1: Restyle FileChip to Light Theme

**Files:**
- `apps/web/src/components/chat/FileChip.tsx`

**Changes:**
- Update all color classes to light theme
- Keep same structure and behavior

**Acceptance Criteria:**
- [x] FileChip uses light background (`bg-gray-100` or white with border)
- [x] Text is dark (`text-gray-900`)
- [x] Icons use appropriate colors (blue for document, green for check)
- [x] Error state uses light red tint (`bg-red-50`)
- [x] Visual consistency with rest of app

---

### Story 6.8.2: Add Attachments Field to Message Schema

**Files:**
- `packages/backend/src/domain/entities/Message.ts`
- `packages/backend/src/infrastructure/database/schema/messages.ts`
- Generate migration

**Changes:**
- Add `MessageAttachment` interface
- Add `attachments` field to Message entity
- Add `attachments` jsonb column to messages table

**Acceptance Criteria:**
- [x] MessageAttachment type defined
- [x] Message entity includes optional attachments
- [x] Database schema updated with migration
- [x] Existing messages unaffected (nullable field)

---

### Story 6.8.3: Backend - Accept Attachments in Message Creation

**Files:**
- `packages/backend/src/infrastructure/websocket/ChatServer.ts`
- `packages/backend/src/application/services/ConversationService.ts`

**Changes:**
- Update `sendMessage` handler to accept attachments array
- Store attachments with message in database

**Acceptance Criteria:**
- [x] WebSocket `send_message` event accepts attachments payload
- [x] Attachments stored with message in database
- [x] Message broadcast includes attachments

---

### Story 6.8.4: Backend - File Download Endpoint

**Files:**
- `packages/backend/src/infrastructure/http/controllers/DocumentUploadController.ts`
- `packages/backend/src/infrastructure/http/routes/documents.ts`

**Changes:**
- Add `GET /api/documents/:fileId/download` endpoint
- Verify user owns conversation containing the file
- Stream file from storage with proper headers

**Acceptance Criteria:**
- [x] Download endpoint returns file with correct Content-Type
- [x] Content-Disposition header triggers browser download
- [x] Authorization check (user must own conversation)
- [x] 404 for non-existent files

---

### Story 6.8.5: Frontend - FileChipInChat Component

**Files:**
- `apps/web/src/components/chat/FileChipInChat.tsx` (NEW)
- `apps/web/src/components/chat/__tests__/FileChipInChat.test.tsx` (NEW)

**Props:**
```typescript
interface FileChipInChatProps {
  filename: string;
  fileId: string;
  mimeType?: string;
  onClick: () => void; // Trigger download
}
```

**Design:**
- Light theme (matches Story 6.8.1)
- Document icon + filename + "Document" label
- No X button
- No progress bar
- Cursor pointer, hover state
- Clickable triggers download

**Acceptance Criteria:**
- [x] Renders filename with document icon
- [x] Light theme styling
- [x] Click triggers onClick callback
- [x] Hover state indicates clickability
- [x] Unit tests for rendering and click

---

### Story 6.8.6: Frontend - MessageBubble Renders Attachments

**Files:**
- `apps/web/src/components/chat/MessageBubble.tsx`

**Changes:**
- Accept `attachments` prop
- If user message has attachments, render FileChipInChat for each
- Wire onClick to download handler

**Acceptance Criteria:**
- [x] User messages with attachments show FileChipInChat
- [x] Attachments appear above message text (or standalone if no text)
- [x] Click triggers file download
- [x] Multiple attachments supported (future-proof)

---

### Story 6.8.7: Frontend - Composer Sends Attachment with Message

**Files:**
- `apps/web/src/components/chat/Composer.tsx`
- `apps/web/src/hooks/useFileUpload.ts`

**Changes:**
1. `useFileUpload` exposes `getAttachmentForSend()` returning file metadata or null
2. `Composer.handleSend()` includes attachment in message payload
3. After successful send, call `reset()` to clear FileChip

**Flow:**
```typescript
const handleSend = () => {
  const attachment = getAttachmentForSend(); // { fileId, filename, ... } or null
  onSendMessage(trimmedMessage, attachment ? [attachment] : undefined);
  reset(); // Clear file chip
};
```

**Acceptance Criteria:**
- [x] Send includes attachment metadata when file is ready
- [x] FileChip clears from composer after send
- [x] Works with text + file, or file-only (empty message)
- [x] Works with text-only (no file)

---

### Story 6.8.8: Frontend - Download Handler

**Files:**
- `apps/web/src/hooks/useFileDownload.ts` (NEW) or inline in ChatInterface

**Implementation:**
```typescript
const downloadFile = async (fileId: string, filename: string) => {
  const response = await fetch(`/api/documents/${fileId}/download`, {
    headers: { Authorization: `Bearer ${token}` },
  });
  const blob = await response.blob();
  const url = URL.createObjectURL(blob);
  const a = document.createElement('a');
  a.href = url;
  a.download = filename;
  a.click();
  URL.revokeObjectURL(url);
};
```

**Acceptance Criteria:**
- [x] Clicking file in chat triggers download
- [x] File downloads with original filename
- [x] Auth token included in request
- [x] Error handling for failed downloads

---

## Implementation Order

```
6.8.1 (FileChip light theme) - Independent, can start immediately
6.8.2 (Schema) → 6.8.3 (Backend accept) → 6.8.4 (Download endpoint)
6.8.5 (FileChipInChat) - Depends on 6.8.1 for styling reference
6.8.6 (MessageBubble) - Depends on 6.8.5
6.8.7 (Composer send) - Depends on 6.8.3
6.8.8 (Download handler) - Depends on 6.8.4
```

**Parallel tracks:**
- Track A: 6.8.1 → 6.8.5 → 6.8.6 (Frontend components)
- Track B: 6.8.2 → 6.8.3 → 6.8.4 (Backend)
- Track C: 6.8.7, 6.8.8 (Integration, after tracks A & B)

---

## Testing Checklist

### Manual Testing
- [x] Upload file → FileChip appears in composer (light theme)
- [x] Send message with file → FileChip moves to chat stream
- [x] FileChip in chat shows filename, document icon
- [x] Click FileChip in chat → File downloads
- [x] Send another message → Appears below file attachment
- [x] Scroll up → File attachment still visible in history
- [x] Send text-only message → No file chip, normal behavior
- [x] Upload new file after sending previous → Works correctly

### Automated Testing
- [x] FileChip light theme unit tests
- [x] FileChipInChat unit tests (render, click)
- [x] MessageBubble with attachments test
- [x] Backend message creation with attachments
- [x] Download endpoint auth and file serving

---

## Files Summary

| File | Action | Story |
|------|--------|-------|
| `FileChip.tsx` | Modify (restyle) | 6.8.1 |
| `Message.ts` (domain) | Modify (add attachments) | 6.8.2 |
| `messages.ts` (schema) | Modify (add column) | 6.8.2 |
| `ChatServer.ts` | Modify (accept attachments) | 6.8.3 |
| `ConversationService.ts` | Modify (store attachments) | 6.8.3 |
| `DocumentUploadController.ts` | Modify (download endpoint) | 6.8.4 |
| `documents.ts` (routes) | Modify (add route) | 6.8.4 |
| `FileChipInChat.tsx` | Create | 6.8.5 |
| `MessageBubble.tsx` | Modify (render attachments) | 6.8.6 |
| `Composer.tsx` | Modify (send with attachment) | 6.8.7 |
| `useFileUpload.ts` | Modify (expose attachment getter) | 6.8.7 |
| `useFileDownload.ts` | Create | 6.8.8 |

---

## Notes

1. **MVP Scope:** This story focuses on the core UX pattern. Advanced features like drag-and-drop, multi-file, or inline preview are out of scope.

2. **Backward Compatibility:** Existing messages without attachments are unaffected (nullable field).

3. **File Storage:** Uses existing file storage from Epic 16. No changes to how files are stored.

4. **Security:** Download endpoint must verify user owns the conversation containing the file attachment.
