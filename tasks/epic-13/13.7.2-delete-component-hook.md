# Story 13.7.2 - Delete Component and Hook Files

## Status: Not Started

## Context
After ChatInterface no longer references the sticky indicator, we can safely delete the component and hook files.

## Goal
Delete the sticky indicator component and visibility hook source files.

## Files to Delete

### 1. StickyQuestionnaireIndicator Component
```
apps/web/src/components/chat/StickyQuestionnaireIndicator.tsx
```
- 110 lines
- 40px bar that appeared above Composer
- No longer imported anywhere

### 2. useQuestionnaireCardVisibility Hook
```
apps/web/src/hooks/useQuestionnaireCardVisibility.ts
```
- 43 lines
- IntersectionObserver-based visibility tracking
- No longer imported anywhere

## Implementation

### Delete Commands
```bash
rm apps/web/src/components/chat/StickyQuestionnaireIndicator.tsx
rm apps/web/src/hooks/useQuestionnaireCardVisibility.ts
```

### Verification

After deletion, verify no import errors:
```bash
pnpm --filter @guardian/web build
```

If build fails with import errors, there are remaining references that need cleanup.

## Additional Cleanup - Barrel Exports

After deleting files, check for barrel exports that reference them:

### Check Component Index
```bash
# Check if there's a components/chat/index.ts that exports StickyQuestionnaireIndicator
grep -r "StickyQuestionnaireIndicator" apps/web/src/components/
```

### Check Hooks Index
```bash
# Check if there's a hooks/index.ts that exports useQuestionnaireCardVisibility
grep -r "useQuestionnaireCardVisibility" apps/web/src/hooks/
```

If found, remove those exports from the barrel files.

## Acceptance Criteria
- [ ] Delete `StickyQuestionnaireIndicator.tsx`
- [ ] Delete `useQuestionnaireCardVisibility.ts`
- [ ] Remove barrel exports from `index.ts` files (if any)
- [ ] No dangling exports/imports
- [ ] No import errors in build
- [ ] Build passes: `pnpm --filter @guardian/web build`

## Testing
After implementation:
```bash
pnpm --filter @guardian/web build
```

## Dependencies
- Story 13.7.1 (ChatInterface cleanup must be done first)

## Blocked By
- Story 13.7.1
