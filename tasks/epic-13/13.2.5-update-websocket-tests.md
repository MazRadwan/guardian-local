# Story 13.2.5 - Update WebSocket Events Tests

## Status: Not Started

## Context
Story 13.2.3 removed the `clearDismiss()` call from `handleQuestionnaireReady` in useWebSocketEvents. The tests need to be updated to reflect this change.

## Goal
Update WebSocket events tests to remove assertions for `clearDismiss` being called while keeping `savePayload` assertions.

## File to Modify
`apps/web/src/hooks/__tests__/useWebSocketEvents.test.ts` (if exists)

Note: If this test file doesn't exist, check for tests in:
- `apps/web/src/hooks/__tests__/`
- `apps/web/__tests__/hooks/`
- Integration tests that cover websocket events

## Current Test Coverage
Tests for `handleQuestionnaireReady` likely verify:
1. Persistence calls (dismiss and payload)
2. Store state updates
3. Guard conditions (wrong conversation, generation in progress)

## Required Changes

### 1. Remove clearDismiss Assertions
Find and update tests that verify `clearDismiss` is called:

**Before:**
```typescript
it('should clear dismiss flag and save payload when questionnaire_ready received', () => {
  // ... setup ...

  // Emit event
  mockSocket.emit('questionnaire_ready', payload);

  // Assert
  expect(mockPersistence.clearDismiss).toHaveBeenCalledWith('conv-123');  // REMOVE
  expect(mockPersistence.savePayload).toHaveBeenCalledWith('conv-123', payload);
});
```

**After:**
```typescript
it('should save payload when questionnaire_ready received', () => {
  // ... setup ...

  // Emit event
  mockSocket.emit('questionnaire_ready', payload);

  // Assert
  expect(mockPersistence.savePayload).toHaveBeenCalledWith('conv-123', payload);
  // clearDismiss no longer called
});
```

### 2. Update Test Names
Update test names to reflect new behavior:

**Before:**
```typescript
it('should clear dismiss flag and save payload...')
```

**After:**
```typescript
it('should save payload to localStorage...')
```

### 3. Keep Other Assertions
Keep all other assertions unchanged:
- Store state updates (`setPendingQuestionnaire`, `setQuestionnaireUIState`)
- Guard conditions
- Error handling

## Test Structure After Changes

```typescript
describe('useWebSocketEvents', () => {
  describe('handleQuestionnaireReady', () => {
    it('should save payload to localStorage', () => {
      // Verify savePayload called with correct args
    });

    it('should update store state to ready', () => {
      // Verify setPendingQuestionnaire and setQuestionnaireUIState
    });

    it('should clear any existing error', () => {
      // Verify setQuestionnaireError(null)
    });

    it('should ignore events for different conversation', () => {
      // Guard test
    });

    it('should ignore events if generation already in progress', () => {
      // Guard test
    });

    // NO clearDismiss tests
  });

  // Other handlers: handleExportReady, handleExtractionFailed, etc.
});
```

## If Test File Doesn't Exist
If there are no dedicated tests for useWebSocketEvents:

1. **Option A**: Create minimal tests for the changed behavior
2. **Option B**: Note that coverage exists via integration/e2e tests
3. **Option C**: Skip this story if websocket events are tested elsewhere

Check for coverage in:
```bash
pnpm --filter @guardian/web test -- --coverage
```

Look at coverage report for `useWebSocketEvents.ts`.

## Acceptance Criteria
- [ ] All `clearDismiss` assertions removed
- [ ] `savePayload` assertions preserved
- [ ] All tests pass
- [ ] No TypeScript errors in tests
- [ ] Coverage maintained (if tests exist)

## Testing
Run the updated tests:
```bash
pnpm --filter @guardian/web test -- useWebSocketEvents
```

Or if named differently:
```bash
pnpm --filter @guardian/web test -- websocket
```

All tests should pass after updates.

## Dependencies
- Story 13.2.3 (clearDismiss call removed)

## Blocked By
- Story 13.2.3
