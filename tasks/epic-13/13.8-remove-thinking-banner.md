[1 tool called]

Use this as the handoff prompt for the agent:

---

**Story 13.8 – Remove Composer “Guardian is thinking” Banner**

Goal: Eliminate the purple “Guardian is thinking” status shown during quick conversation switches/pre-stream loading. It’s a brief flicker and no longer needed.

Scope:
- Frontend only (apps/web).
- Likely touch:
  - `apps/web/src/components/chat/Composer.tsx` (remove the `showThinking` conditional block and any related prop/state)
  - Upstream props in `apps/web/src/components/chat/ChatInterface.tsx` or hooks if they pass `isLoading`/`showThinking` specifically for this banner (ensure no regressions to other loading behaviors)

Requirements:
- Remove rendering of the “Guardian is thinking” banner and its bouncing dots.
- Ensure the composer still handles disabled/loading states appropriately for streaming/send; don’t regress send/stop behavior.
- Keep other loading indicators intact (e.g., chat skeletons) if they’re unrelated.
- Maintain selectors/testids for remaining controls; remove banner-specific testids if present.

Non-goals:
- Sticky indicator removal (13.7).
- Stepper UX.
- Download durability/rehydration changes.

Testing:
- Update/delete component tests that assert the banner exists.
- Verify composer snapshots and interaction tests still pass (send/stop/disable logic unaffected).

Deliverables:
- Banner removed; composer retains normal send/stream behavior and passes updated tests.

---

## Sub-Stories

| Story | Title | Status | File |
|-------|-------|--------|------|
| 13.8.1 | Remove Thinking Banner from Composer | Not Started | [13.8.1-remove-thinking-banner.md](./13.8.1-remove-thinking-banner.md) |
| 13.8.2 | Clean Up showThinking Variable | Not Started | [13.8.2-cleanup-showthinking-variable.md](./13.8.2-cleanup-showthinking-variable.md) |
| 13.8.3 | Verify Composer Behavior | Not Started | [13.8.3-verify-composer-behavior.md](./13.8.3-verify-composer-behavior.md) |

### Dependency Graph
```
13.8.1 (remove banner) → 13.8.2 (cleanup variable) → 13.8.3 (verify)
```

### Summary of Sub-Stories

1. **13.8.1 - Remove Thinking Banner**: Delete the `{showThinking && (...)}` JSX block (lines 145-161) containing the purple circle icon and "Guardian is thinking" text with bouncing dots.

2. **13.8.2 - Clean Up showThinking Variable**: If `showThinking` is only used for the deleted banner, remove the variable declaration (`const showThinking = isLoading && !isStreaming`). Keep `isBusy`, `isLoading`, and `isStreaming` which are used elsewhere.

3. **13.8.3 - Verify Composer Behavior**: Verify send/stop/disabled behaviors unchanged. Ensure MessageList typing indicator (different component) still works.

### Files Summary

| Action | File |
|--------|------|
| MODIFY | `Composer.tsx` (remove banner, optionally remove variable) |
| UNCHANGED | `ChatInterface.tsx` |
| UNCHANGED | `MessageList.tsx` (typing indicator is different) |
| UNCHANGED | `Composer.test.tsx` (no tests reference banner) |

### What NOT to Remove

**Keep these intact:**
1. **MessageList Typing Indicator** - Purple circle + bouncing dots in message area (testid: `typing-indicator`), shows during streaming
2. **Chat Skeleton Loaders** - Shows when `messages.length === 0 && isLoading`
3. **Stop Button** - Shows during `isStreaming`
4. **isLoading Prop** - Still used for disabled state and hiding tools
