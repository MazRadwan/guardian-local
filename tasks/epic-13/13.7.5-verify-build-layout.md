# Story 13.7.5 - Verify Layout and Build

## Status: Not Started

## Context
After removing the sticky indicator, verify that the build passes, tests pass, and the layout looks correct without any visual regressions.

## Goal
Final verification that the cleanup is complete and has no regressions.

## Verification Steps

### 1. TypeScript Build Check
```bash
pnpm --filter @guardian/web build
```
- Should complete without errors
- No import errors for deleted files
- No type errors
- No dangling TS path aliases (e.g., `@/hooks/useQuestionnaireCardVisibility`)
- Tree-shaking doesn't leave orphaned references

### 2. Test Suite
```bash
pnpm --filter @guardian/web test
```
- All tests should pass
- No import errors in test files
- Test count will be lower (removed ~31 test cases)

### 3. Visual Verification (Manual)
```bash
pnpm --filter @guardian/web dev
```

Check the following:
- [ ] Chat messages scroll correctly
- [ ] Composer appears at bottom (no gap above it)
- [ ] No 40px space where indicator used to be
- [ ] Inline QuestionnairePromptCard renders in message list
- [ ] Card stepper works (if generation in progress)
- [ ] Download buttons work (if generation complete)
- [ ] No console errors in browser DevTools

### 4. Layout Structure Verification

Expected layout after cleanup:
```
ChatInterface
  ├── Error Banner (if error)
  ├── MessageList (scrollable)
  │   ├── Messages
  │   └── QuestionnairePromptCard (inline, when active)
  │
  └── Bottom Bar (sticky)
      ├── Composer  <-- No indicator above this
      └── Footer text
```

### 5. Padding/Margin Check

The sticky indicator was 40px tall (`h-10`). After removal:
- Verify no 40px gap above Composer
- Verify MessageList bottom padding hasn't changed unexpectedly
- Verify chat scroll behavior is correct (messages don't hide behind Composer)

Check in DevTools:
1. Inspect Composer container
2. Verify no extra margin-top or padding-top
3. Verify MessageList has correct padding-bottom for scroll clearance

### 6. Functionality Preserved

Verify inline card still works:
1. Start a conversation
2. Trigger questionnaire generation
3. Verify card appears inline in message list
4. Verify stepper shows progress
5. Verify download buttons appear when complete
6. Verify downloads work

## Acceptance Criteria
- [ ] `pnpm build` passes with no errors
- [ ] No dangling TS path aliases or orphaned references
- [ ] `pnpm test` passes with no failures
- [ ] No console errors in browser
- [ ] Layout visually correct (no 40px gap above composer)
- [ ] MessageList padding/margins unchanged
- [ ] Chat scroll behavior works correctly
- [ ] Inline questionnaire card renders correctly
- [ ] Card stepper shows generation progress
- [ ] Download buttons functional
- [ ] No visual regressions

## Testing
```bash
# Build verification
pnpm --filter @guardian/web build

# Test verification
pnpm --filter @guardian/web test

# Manual verification
pnpm --filter @guardian/web dev
```

## Dependencies
- Stories 13.7.1-13.7.4 (all cleanup complete)

## Blocked By
- Story 13.7.4
