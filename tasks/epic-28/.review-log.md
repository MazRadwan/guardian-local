# Epic 28 Review Log

## Spec Design Review Log

### Sprint Batch Review Schedule

| Batch | Sprints | Status | GPT Calls |
|-------|---------|--------|-----------|
| 1 | Sprint 1-2 | **APPROVED** | 1 |
| 2 | Sprint 3-4 | **APPROVED** | 1 |
| 3 | Sprint 5-6 | **APPROVED** | 1 |

---

## Review History

### Batch 1: Sprints 1-2 (Foundation + Infrastructure)

**Submitted:** 2026-01-19T21:30:00Z
**Status:** APPROVED (with 1 minor fix applied)

#### Review Summary

**Overall Assessment:** APPROVED

**Strengths:**
1. Clear sequencing - utilities extracted first (low risk), then context builders, then infrastructure interfaces
2. CRITICAL constraint explicitly documented: `user:{userId}` room join for DocumentUploadController
3. Files Touched sections are specific and enable parallelization
4. Comprehensive test requirements with actual test code examples

**Issues Found:**

```
**Issue #1: Story 28.1.4 function name mismatch**
- Story: 28.1.4
- Severity: Minor
- Problem: Spec references `isValidVendorName` but actual function in QuestionnaireReadyService is `validateVendorName` with different signature (returns string|null, not boolean)
- Resolution: Updated story to match actual codebase - renamed function reference and adjusted approach to handle differing signatures
```

**Verification:**
- Confirmed `sanitizeForPrompt` exists as private method in ChatServer.ts:227
- Confirmed `sanitizeErrorForClient` exists as private method in ChatServer.ts:242
- Confirmed `isValidVendorName` exists as private method in ChatServer.ts:289
- Confirmed `validateVendorName` exists in QuestionnaireReadyService.ts:78 (different signature)
- Confirmed `user:{userId}` room join at ChatServer.ts:1314

**Verdict:** <promise>SPRINT_BATCH_APPROVED</promise>

---

### Batch 2: Sprints 3-4 (Conversation + Business Logic)

**Submitted:** 2026-01-19T21:45:00Z
**Status:** APPROVED (with fixes applied)

#### Review Summary

**Overall Assessment:** APPROVED

**Strengths:**
1. Clear separation between ConversationHandler and ModeSwitchHandler
2. Centralized ownership validation (Story 28.5.4) reduces code duplication
3. Scoring handler properly handles multi-vendor clarification flow
4. QuestionnaireHandler preserves public method interface for test compatibility

**Issues Found:**

```
**Issue #1: Mode event name mismatch**
- Story: 28.6.1, 28.6.2
- Severity: Minor
- Problem: Stories used 'mode_switched' but actual event is 'conversation_mode_updated'
- Resolution: Updated story specs to match codebase

**Issue #2: Guidance message persistence**
- Story: 28.6.2
- Severity: Minor
- Problem: Story originally showed guidance as separate event, but actual implementation persists as assistant message
- Resolution: Updated to show correct persistence and emission via 'message' event
```

**Verification:**
- Confirmed `switch_mode` handler at ChatServer.ts:2013
- Confirmed `conversation_mode_updated` event at ChatServer.ts:2055
- Confirmed guidance messages persisted via conversationService.sendMessage at ChatServer.ts:2079, 2119
- Confirmed idempotent handling at ChatServer.ts:2044-2051

**Verdict:** <promise>SPRINT_BATCH_APPROVED</promise>

---

### Batch 3: Sprints 5-6 (Message Flow + Final Integration)

**Submitted:** 2026-01-19T22:00:00Z
**Status:** APPROVED (with 1 minor fix applied)

#### Review Summary

**Overall Assessment:** APPROVED

**Strengths:**
1. MessageHandler extraction is well-broken-down into validation, attachments, mode config, and streaming
2. ToolUseRegistry properly uses existing IToolUseHandler interface from application layer
3. Mode strategies (optional) follow clean strategy pattern
4. Final integration story has clear verification checklist

**Issues Found:**

```
**Issue #1: IToolUseHandler interface mismatch**
- Story: 28.9.3
- Severity: Minor
- Problem: Original story used simplified interface, but actual IToolUseHandler has richer types (ToolUseInput, ToolUseResult, ToolUseContext)
- Resolution: Updated story to use actual interface types from application/interfaces/IToolUseHandler.ts
```

**Verification:**
- Confirmed IToolUseHandler interface at application/interfaces/IToolUseHandler.ts
- Confirmed QuestionnaireReadyService implements IToolUseHandler
- Architecture constraint verified: registry in infrastructure, handlers in application

**Verdict:** <promise>SPRINT_BATCH_APPROVED</promise>

---

## Final Pass Review

**Status:** APPROVED
**Completed:** 2026-01-19T22:10:00Z

### Cross-Sprint Consistency Check

| Check | Status |
|-------|--------|
| All story files exist | ✓ 44 stories across 6 sprints |
| Files Touched sections specific | ✓ |
| Agent assignments consistent | ✓ All backend-agent |
| Sprint dependencies correct | ✓ Sequential (1→2→3→4→5→6) |
| Architecture constraints consistent | ✓ ChatContext, IAuthenticatedSocket, tool registry |
| Test requirements included | ✓ |

### Story Count Verification

| Sprint | Stories | Verified |
|--------|---------|----------|
| Sprint 1 | 8 | ✓ |
| Sprint 2 | 7 | ✓ |
| Sprint 3 | 8 | ✓ |
| Sprint 4 | 7 | ✓ |
| Sprint 5 | 6 | ✓ |
| Sprint 6 | 8 | ✓ |
| **Total** | **44** | ✓ |

### Architecture Constraints Verified Across Sprints

1. **ChatContext is infrastructure-only** - Stories 28.3.1, 28.3.3 ✓
2. **IAuthenticatedSocket interface** - Stories 28.3.2, 28.4.x ✓
3. **Tool registry in infrastructure** - Story 28.9.3 ✓
4. **user:{userId} room join preserved** - Story 28.4.2 ✓
5. **Services via constructor DI** - Stories 28.11.1, 28.11.3 ✓

### Final Verdict

<promise>FINAL_PASS_APPROVED</promise>

All 6 sprints (44 stories) are approved and ready for implementation

---

## Reviewer Feedback Incorporation

### Feedback Received: 2026-01-19 (Post-Approval)

**Source:** External reviewer analysis of Sprint 1-2 specifications

#### Recommendations Received

1. **Sanitization semantics preservation** (ChatServer.ts → utils/sanitize.ts)
   - Problem: ChatServer uses whitespace normalization (`.replace(/\s+/g, ' ')`) and 200-char default; utils/sanitize.ts doesn't
   - Resolution: Updated Story 28.1.3 with `CHAT_CONTEXT_PROFILE` preset and `normalizeWhitespace` option
   - **IMPORTANT NOTE**: The `normalizeWhitespace` option exists **solely to preserve legacy behavior**. Do NOT remove or "clean up" this option in future refactors - it prevents regressions in context formatting and prompt-injection protections.

2. **SQL-leak suppression tests**
   - Problem: sanitizeErrorForClient() needs explicit SQL pattern filtering tests
   - Resolution: Updated Story 28.1.1 with SQL suppression test cases

3. **Story 26.2 vendor validation lock-in**
   - Problem: isValidVendorName() tests should document exact validation rules
   - Resolution: Updated Story 28.1.2 with behavior lock-in documentation

4. **utils/sanitize.ts dependency-free constraint**
   - Problem: Application-layer code (QuestionnaireReadyService) must be able to import
   - Resolution: Added explicit constraint to Stories 28.1.1, 28.1.2, 28.1.3

5. **StreamingHandler abort handling**
   - Problem: Must preserve `assistant_aborted` vs `assistant_done` semantics
   - Resolution: Updated Story 28.1.5 with explicit abort path tests and chunk edge cases

6. **Context builder coverage**
   - Problem: Need tests for S3 fallback/backfill and multi-doc/legacy context injection
   - Resolution: Updated Story 28.2.2 with comprehensive test scenarios

#### Stories Updated

| Story | Update Summary |
|-------|----------------|
| 28.1.1 | Added SQL-leak suppression tests, dependency-free constraint |
| 28.1.2 | Added Story 26.2 behavior lock-in, dependency-free constraint |
| 28.1.3 | Added `normalizeWhitespace` option, `CHAT_CONTEXT_PROFILE`, new tests |
| 28.1.4 | Added Story 26.2 parity test for numeric/option-token rejection |
| 28.1.5 | Added `assistant_aborted` vs `assistant_done` tests, chunk edge cases |
| 28.2.2 | Added S3 fallback, multi-doc, and legacy context tests |

---

### Sprint 3-4 Feedback (2026-01-19)

**Source:** External reviewer analysis of Sprint 3-4 specifications

#### Approvals

- Phase 3 (ChatContext) cleanly models cross-cutting state and preserves infrastructure-only constraint
- Phase 4 (ConnectionHandler extraction) correctly targets auth, resume logic, and room join behavior with explicit integration-test gating

#### Recommendations Received

1. **IAuthenticatedSocket lifecycle mismatch**
   - Problem: userId was readonly but needs to be writable (set by auth middleware); interface lacked handshake.auth shape
   - Resolution: Updated Story 28.3.2 with optional/writable userId and `ISocketHandshakeAuth` interface

2. **connection_ready payload fields**
   - Problem: Acceptance criteria didn't enumerate all fields, risking silent frontend regressions
   - Resolution: Updated Story 28.4.2 with explicit field list: message, userId, conversationId, resumed, hasActiveConversation, assessmentId

3. **Pending-conversation cleanup preservation**
   - Problem: Cleanup interval for pendingCreations could be accidentally removed when moving to ChatContext
   - Resolution: Updated Story 28.3.3 with CRITICAL note and explicit code showing cleanup interval migration

#### Stories Updated

| Story | Update Summary |
|-------|----------------|
| 28.3.2 | Made userId optional/writable, added `ISocketHandshakeAuth` interface |
| 28.3.3 | Added pending-conversation cleanup interval preservation note |
| 28.4.2 | Expanded connection_ready payload field enumeration |

---

### Sprint 4-6 Feedback (2026-01-19)

**Source:** External reviewer analysis of Sprint 4-6 specifications

#### Approvals

- Sprint 4 (ConnectionHandler) aligns with current ChatServer connection flow and preserves critical user room semantics
- Sprint 5 (ConversationHandler) maps cleanly to existing event responsibilities and keeps ownership validation centralized
- Sprint 6 (ModeSwitchHandler) accurately captures current switch_mode behavior including guidance messaging

#### Recommendations Received

1. **Resume failure behavior preservation** (Sprint 4)
   - Problem: Need to ensure resume failures do NOT auto-create conversations
   - Resolution: Updated Story 28.4.2 with explicit criteria that socket.conversationId remains unset on failure

2. **Auth middleware failure tests** (Sprint 4)
   - Problem: Need tests for missing/invalid/expired/malformed tokens
   - Resolution: Updated Story 28.4.1 with comprehensive auth failure test cases

3. **start_new_conversation idempotency details** (Sprint 5)
   - Problem: Subtle behavior worth preserving - 200ms guard, error cleanup, always consult mode
   - Resolution: Updated Story 28.5.2 with locked-in idempotency behavior and tests

4. **delete_conversation idempotent success** (Sprint 5)
   - Problem: Should emit conversation_deleted even when not found, clear socket.conversationId
   - Resolution: Updated Story 28.5.2 with idempotent delete behavior and socket cleanup tests

5. **Mode switch idempotency** (Sprint 6)
   - Problem: conversation_mode_updated should emit even on idempotent switches
   - Resolution: Updated Story 28.6.1 with idempotent switch test

6. **Guidance message persistence** (Sprint 6)
   - Problem: Guidance persisted via conversationService.sendMessage, emitted as message event
   - Resolution: Updated Story 28.6.2 with explicit persistence and emission criteria

#### Stories Updated

| Story | Update Summary |
|-------|----------------|
| 28.4.1 | Added auth failure cases (expired, malformed, wrong secret tokens) |
| 28.4.2 | Added resume failure behavior, socket.conversationId unset on failure |
| 28.5.2 | Added 200ms guard, consult mode lock-in, error cleanup, idempotent delete |
| 28.6.1 | Added idempotent mode switch test |
| 28.6.2 | Clarified guidance persistence via sendMessage and emission via message event |

---

---

## Final Pass Corrections (2026-01-19)

**Status:** COMPLETE

### Corrections Completed

| Story | Issue | Resolution |
|-------|-------|------------|
| 28.2.1 (sprint-1-story-6) | Missing intake-context injection, history limit 10, empty filter, promptCache | Added multi-doc and legacy context injection, history limit of 10, empty message filtering, PromptCacheManager.ensureCached() |
| 28.2.2 (sprint-1-story-7) | Wrong output format, repository method, missing S3 backfill | Changed to `--- Attached Documents ---` header, `findByConversationWithExcerpt`, lazy backfill via `updateTextExcerpt`, MIME_TYPE_MAP |
| 28.5.2 (sprint-3-story-2) | Wrong event name `conversation_started` | Changed to `conversation_created` with full metadata `{ conversation: { id, title, createdAt, updatedAt, mode } }` |
| 28.5.3 (sprint-3-story-3) | Wrong event `conversation_history`, socket.conversationId fallback | Changed to `history` event, removed fallback, added idempotent empty history, attachment shaping |

### Corrections Completed (2026-01-19)

#### Sprint 4 Stories 1, 3 (ScoringHandler) - DONE

**Fixed in sprint-4-story-1.md:**
- Added vendor clarification map in `socket.data.pendingVendorClarifications` (Map keyed by conversationId)
- Added `tryStartParsing()` idempotency guard
- Added `NOT_A_QUESTIONNAIRE` short-circuit for `file.detectedDocType === 'document'`
- Added `scoring_started` event emission
- Added `scoring_error` with `code` field
- Added `scoring_progress` with `fileId` field
- Corrected scoring callback signature: `(event: ScoringProgressEvent) => void`

**Fixed in sprint-4-story-3.md:**
- Lookup by conversationId in `socket.data.pendingVendorClarifications` Map
- Vendor name normalization (trim, lowercase)
- Clear pending clarification after selection
- Emit confirmation message before resuming

#### Sprint 4 Stories 5-6 (QuestionnaireHandler) - DONE

**Fixed in sprint-4-story-5.md:**
- Corrected service name: `QuestionnaireGenerationService` (not QuestionnaireService)
- Added `assistant_stream_start` before generation
- Added all 4 `generation_phase` events: context, generating, validating, saving
- Added ownership validation via `validateConversationOwnership`
- Added message persistence for user action and assistant response
- Added title upgrade with `isValidVendorName` validation
- Added `export_ready` with assessmentId, questionCount, formats

**Fixed in sprint-4-story-6.md:**
- Added `export_status_not_found` event
- Added `export_status_error` event with specific error messages
- Added ownership validation
- Added conversationId validation (non-empty string)

#### Sprint 5 Stories 1, 4, 5 (MessageHandler) - DONE

**Fixed in sprint-5-story-1.md:**
- Support both `text` and `content` fields (`payload.text || payload.content`)
- Correct rate-limiter API: `isRateLimited(userId)` and `getResetTime(userId)`
- File-only message support with `generatePlaceholderText()` method
- Attachment validation via `findByIdAndConversation(fileId, conversationId)`
- No fallback to `socket.conversationId` - must be from payload

**Fixed in sprint-5-story-4.md:**
- Tools ONLY in assessment mode (`enableTools: true` only for assessment)
- Scoring mode bypasses Claude (`bypassClaude: true`)
- Added `shouldBypassClaude()` and `shouldAutoSummarize()` methods

**Fixed in sprint-5-story-5.md:**
- Async iterator streaming: `for await (const chunk of claudeClient.streamMessage(...))`
- Abort check inside loop: `if (socket.data.abortRequested) break;`
- `message_sent` event after saving user message
- `assistant_stream_start` before streaming
- `assistant_done` suppressed on abort
- Partial response saved on abort
- Tool uses collected from final chunk

### Preserve Notes (CRITICAL)

These behaviors MUST be preserved in handler extraction:

1. **Placeholder text for file-only messages**:
   ```typescript
   let finalMessageText = messageText || '';
   if (!finalMessageText && enrichedAttachments && enrichedAttachments.length > 0) {
     const fileNames = enrichedAttachments.map(a => a.filename).join(', ');
     finalMessageText = `[Uploaded file for analysis: ${fileNames}]`;
   }
   ```

2. **`message_sent` event** - Emitted immediately after saving user message:
   ```typescript
   socket.emit('message_sent', {
     messageId: message.id,
     conversationId: message.conversationId,
     timestamp: message.createdAt,
     attachments: enrichedAttachments,
   });
   ```

3. **`assistant_done` suppression on abort**:
   - If `socket.data.abortRequested === true`, do NOT emit `assistant_done`
   - Partial response is still saved to DB

4. **`stream_aborted` vs abort semantics**:
   - `socket.on('abort_stream')` sets `socket.data.abortRequested = true`
   - Also adds conversationId to `abortedStreams` set
   - Emits `stream_aborted` event as acknowledgment

---

## Parallel Execution Planning

**Added:** 2026-01-19

Added comprehensive dependency chart to `epic-28-goals.md` for file-grouping agent orchestration:

### Key Findings

1. **Max parallelization per sprint:**
   - Sprint 1: 4 agents (Phase A)
   - Sprint 2: 3 agents (Phase B)
   - Sprint 3: 4 agents (Phase B)
   - Sprint 4: 3 agents (Phase B)
   - Sprint 5: 3 agents (Phase B)
   - Sprint 6: 3 agents (Phase B)

2. **Critical bottleneck:** ChatServer.ts requires sequential execution across 11 stories (28.1.3 → 28.6.6)

3. **Best parallel opportunities:**
   - Sprint 1 Phase A: 4 independent file creations
   - Sprint 3 Phase B: 4 independent method additions
   - Sprint 6 Phase B: 3 independent strategy implementations

### Chart Location

`tasks/epic-28/epic-28-goals.md` → "Parallel Execution Dependency Chart" section

---

## Re-Review Corrections (2026-01-19)

**Status:** COMPLETE

### Issues Identified by Re-Review

1. **sprint-4-story-2.md (buildScoringFollowUpContext)** - HIGH
   - Problem: Spec used `scoringService.getScoringState(conversationId)` which doesn't exist
   - Actual behavior: `buildScoringFollowUpContext(report)` is a synchronous pure function that takes the scoring report directly
   - Context: Only called when `userQuery` exists after scoring completes

2. **sprint-4-story-1.md (triggerScoringOnSend)** - HIGH
   - Missing behaviors:
     - Dependency gate requires BOTH `scoringService` AND `fileStorage`
     - `resultData` must include `batchId` and `assessmentId`
     - Narrative message persistence + 'message' event emission (NO components)
     - `conversationService.linkAssessment()` call (non-fatal)
     - Follow-up query flow when `userQuery` provided

### Corrections Applied

| Story | Change Summary |
|-------|----------------|
| 28.7.2 (sprint-4-story-2) | Changed from async `getScoringState(conversationId)` to sync `buildScoringFollowUpContext(report)` with report parameter; updated tests to verify formatting output |
| 28.7.1 (sprint-4-story-1) | Added fileStorage to dependency check; added batchId/assessmentId to scoring_complete payload; added narrative persistence + message event; added linkAssessment (non-fatal); added follow-up query flow with Claude streaming; updated constructor and tests |

### Updated Acceptance Criteria

**sprint-4-story-1.md:**
- [x] Dependency gate: Requires BOTH `scoringService` AND `fileStorage`
- [x] `scoring_complete` payload includes `batchId` and `assessmentId`
- [x] Narrative message persisted + 'message' event emitted (NO components)
- [x] `conversationService.linkAssessment()` called (non-fatal on failure)
- [x] Follow-up query flow when `userQuery` provided

**sprint-4-story-2.md:**
- [x] Synchronous pure function signature: `buildScoringFollowUpContext(report)`
- [x] Takes report parameter directly (NOT conversationId)
- [x] Formats composite score, dimension scores (/10), key findings, executive summary

---

## Second Re-Review Corrections (2026-01-19)

**Status:** COMPLETE

### Issues Identified

1. **sprint-5-story-3.md (ToolUseRegistry)** - HIGH
   - Problem: Tests used old API (`register('tool_name', handler)` and `dispatch('tool_name', input, context)`)
   - Fix: Updated tests to use correct API (`register(handler)` using `handler.toolName`, `dispatch(ToolUseInput, ToolUseContext)`)

2. **sprint-5-story-6.md (send_message delegation)** - HIGH
   - Problem: Missing critical behaviors (payload handling, message_sent, scoring bypass, abort handling, tool dispatch types)
   - Fix: Complete rewrite of Technical Approach with all behaviors:
     - `payload.text || payload.content` support
     - `findByIdAndConversation` validation
     - `message_sent` event after save
     - Scoring mode bypass (triggers scoring, NOT Claude)
     - Abort handling with `socket.data.abortRequested`
     - `assistant_done` suppression on abort
     - Tool dispatch with `ToolUseInput`/`ToolUseContext` types
     - Mode-specific post-processing (auto-summarize, background enrichment)

3. **sprint-5-story-2.md (attachment processing)** - MEDIUM
   - Problem: Acceptance said "validates file IDs" but approach didn't validate; input used `fileIds` instead of `attachments`
   - Fix:
     - Renamed to "file context building"
     - Clarified validation is in story 28.9.1
     - Changed method from `processAttachments(fileIds)` to `buildFileContext(enrichedAttachments)`
     - Input now accepts pre-validated `MessageAttachment[]`

### Corrections Applied

| Story | Change Summary |
|-------|----------------|
| 28.9.3 (sprint-5-story-3) | Updated tests to use `handler.toolName` registration and `ToolUseInput`/`ToolUseContext` dispatch types |
| 28.9.6 (sprint-5-story-6) | Complete rewrite with payload handling, message_sent, scoring bypass, abort handling, tool dispatch, mode behaviors |
| 28.9.2 (sprint-5-story-2) | Renamed to file context building; clarified validation in 28.9.1; changed to accept enrichedAttachments |

### Approved Stories

- sprint-4-story-1.md: **APPROVED**
- sprint-4-story-2.md: **APPROVED**

---

---

## Sprint 1 Alignment Corrections (2026-01-19)

**Status:** COMPLETE
**Purpose:** Align Sprint 1 story specs with actual ChatServer.ts implementation

### Issues Identified

1. **sprint-1-story-1.md (sanitizeErrorForClient)** - HIGH
   - Problem: Spec handled both Error instances AND strings; actual implementation (ChatServer.ts:242-275) ONLY handles Error instances
   - Problem: Spec used single regex for SQL patterns; actual uses array of 12 patterns
   - Problem: Spec was missing 200-char truncation
   - Resolution: Updated to match actual implementation exactly

2. **sprint-1-story-2.md (isValidVendorName)** - MEDIUM
   - Problem: Missing option-token rejection pattern from ChatServer.ts:302
   - Resolution: Added `/^(option|choice|select|item|answer)[_\-]?\d*[a-z]?$/i` pattern and tests

3. **sprint-1-story-6.md (ConversationContextBuilder)** - MEDIUM
   - Problem: sanitizeForPrompt calls didn't mention whitespace normalization behavior
   - Resolution: Added CHAT_CONTEXT_PROFILE import and usage notes; updated all sanitizeForPrompt calls

4. **sprint-1-story-7.md (FileContextBuilder)** - MEDIUM
   - Problem: Same as above - sanitizeForPrompt calls lacked whitespace normalization
   - Problem: formatTextExcerptFile should NOT normalize whitespace (preserves document formatting)
   - Resolution: Added profile documentation; formatIntakeContextFile uses CHAT_CONTEXT_PROFILE, formatTextExcerptFile uses escapePromptInjection only

5. **sprint-1-story-8.md (ChatServer wiring)** - HIGH
   - Problem: ConversationContextBuilder instantiation missing fileRepository parameter
   - Resolution: Added `this.fileRepository` as third constructor argument with explanatory note

### Corrections Applied

| Story | Change Summary |
|-------|----------------|
| 28.1.1 (sprint-1-story-1) | Changed to ONLY handle Error instances; added full 12-pattern SQL list; added 200-char truncation; updated all tests |
| 28.1.2 (sprint-1-story-2) | Added option-token rejection pattern; added comprehensive test cases for option1, choice_a, select_2, item3, answer_b variants |
| 28.2.1 (sprint-1-story-6) | Added CHAT_CONTEXT_PROFILE import; added sanitization profile documentation; updated all sanitizeForPrompt calls |
| 28.2.2 (sprint-1-story-7) | Added CHAT_CONTEXT_PROFILE import; documented different profiles for intake vs excerpt; updated sanitizeForPrompt calls |
| 28.2.3 (sprint-1-story-8) | Added fileRepository to ConversationContextBuilder constructor call |

### Key Behavioral Notes

1. **sanitizeErrorForClient** - Does NOT handle string errors (returns fallback for anything that's not Error instance)

2. **isValidVendorName** - Rejects assessment option tokens like "option1", "choice_a", "select_2" to prevent bad tool input

3. **CHAT_CONTEXT_PROFILE** - Preset that includes `normalizeWhitespace: true` to match ChatServer.ts:227-234 private method behavior:
   - Remove control chars ([\x00-\x1F\x7F])
   - Normalize whitespace (\s+ -> single space)
   - Trim
   - Truncate to maxLength (default 200)

4. **formatTextExcerptFile** - Uses `escapePromptInjection: true` but NOT whitespace normalization (preserves document formatting)

---

## Notes

- Created: 2026-01-19
- Workflow: opus-gpt
- Total Stories: 44
- Total Sprints: 6
- Reviewer Feedback Incorporated: 2026-01-19
- Parallel Execution Chart Added: 2026-01-19
- Sprint 1 Alignment Corrections: 2026-01-19
