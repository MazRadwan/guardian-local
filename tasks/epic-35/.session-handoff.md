# Epic 35 Session Handoff

**Status:** Spec Approved by Codex — Ready to Implement
**Branch:** `epic/35-extract-title-generation`
**Created from:** `epic/34-extract-tool-loop` (HEAD at a454a19)
**Last commit:** `f63b30f` spec(epic-35): Add title generation extraction spec (Codex-approved)

---

## NEXT SESSION: Implement Epic 35

**What to do:**
1. Read all spec files in `tasks/epic-35/` (overview, goals, sprint overview, 3 stories)
2. Execute implementation — NO state file, NO file-grouping-agent needed (small epic)
3. Use the parallelization strategy below
4. After implementation: Codex epic-level code review with fix loop
5. Fix loop: recommendations → fix all → re-review → repeat until APPROVED
6. Commit when approved

**Execution order:**
```
Phase 1: 35.1.1 (create interface + implementation) — backend-agent
Phase 2: 35.1.2 + 35.1.3 in PARALLEL — two backend-agents
Post:    Full test suite verification (pnpm test:unit)
```

**35.1.2 and 35.1.3 are PARALLEL** — 35.1.3 tests TitleUpdateService directly with mocked deps, does NOT need ChatServer wiring from 35.1.2. Zero file overlap.

**Codex review scope:** Epic-level (single review of all changes, not per-batch). No pushback loop — accept all recommendations, fix, re-review until approved.

---

## What This Epic Does

Extracts `generateTitleIfNeeded()` and `updateScoringTitle()` from MessageHandler.ts into a dedicated `TitleUpdateService` in the infrastructure layer. Second extraction in the multi-epic MessageHandler decomposition.

## Why This Epic Exists

1. MessageHandler.ts is 1,319 LOC (limit: 300)
2. Title generation is isolated (single dep, fire-and-forget, zero coupling to streaming/tool loop)
3. The regeneration bug (Epic 34) showed tightly-coupled features cascade failures
4. Each extraction reduces risk surface for future changes

## What NOT to Touch

- Any MessageHandler method other than `generateTitleIfNeeded()` and `updateScoringTitle()`
- ConsultToolLoopService (Epic 34)
- ScoringHandler, QuestionnaireHandler
- File upload / export flows

## Source Code Locations

- `MessageHandler.ts:1040-1141` — generateTitleIfNeeded (extract)
- `MessageHandler.ts:1282-1318` — updateScoringTitle (extract)
- `ChatServer.ts:267` — updateScoringTitle call site
- `ChatServer.ts:323` — generateTitleIfNeeded call site
- `ChatServer.ts:158` — MessageHandler constructor (remove titleGenerationService param)

## Sprint Stories

| Story | Name | Phase | Status |
|-------|------|-------|--------|
| 35.1.1 | Create ITitleUpdateService + TitleUpdateService | Phase 1 (solo) | Pending |
| 35.1.2 | Wire into ChatServer, remove from MessageHandler | Phase 2 (parallel) | Pending |
| 35.1.3 | Tests and regression verification | Phase 2 (parallel) | Pending |

## Critical Behaviors to Preserve

1. Scoring mode skip in generateTitleIfNeeded
2. Message count triggers (consult: 2, assessment: 3/5)
3. Vendor info update path (assessment at 5 messages)
4. Manual edit protection (titleManuallyEdited flag)
5. Placeholder detection (isPlaceholderTitle)
6. conversation_title_updated socket event
7. Non-fatal error handling (catch and log)
8. Fire-and-forget pattern (.catch() in ChatServer)
9. Scoring title truncation with extension preservation
10. updateTitleIfNotManuallyEdited atomic check

## Dev Servers

Backend and frontend dev servers may still be running in background from prior session. Check before starting:
- Backend: port 8000
- Frontend: port 3000
